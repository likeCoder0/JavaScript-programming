setTimeout(() => {
    const para = document.createElement("h3");

    // Create a text node:
    const node = document.createTextNode("Initializing Hacking Program...");

    // Append text node to the p element:
    para.appendChild(node);

    // Append the p element to the body:
    document.getElementById("hacking").appendChild(para);
}, 1000);
setTimeout(() => {
    const para = document.createElement("h3");

    // Create a text node:
    const node = document.createTextNode("Hacking Ashish username...");

    // Append text node to the p element:
    para.appendChild(node);

    // Append the p element to the body:
    document.getElementById("hacking").appendChild(para);
}, 2000);
setTimeout(() => {
    const para = document.createElement("h3");

    // Create a text node:
    const node = document.createTextNode("Username found aashish17...");

    // Append text node to the p element:
    para.appendChild(node);

    // Append the p element to the body:
    document.getElementById("hacking").appendChild(para);
}, 3000);
setTimeout(() => {
    const para = document.createElement("h3");

    // Create a text node:
    const node = document.createTextNode("Connecting to facebook...");

    // Append text node to the p element:
    para.appendChild(node);

    // Append the p element to the body:
    document.getElementById("hacking").appendChild(para);
}, 4000);